import styled from "styled-components";

const ButtonInvalid = styled.button`
  background: none;
	color: inherit;
	border: none;
	padding: 0;
	font: inherit;
	cursor: pointer;
	outline: inherit;

  width: 120px;
  height: 39px;
  border-radius: 50px;
  background-color: #FF5A5A;
  color: white;

  margin: 0 auto;
  display: block; 
`;

export { ButtonInvalid }